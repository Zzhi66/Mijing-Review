<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mj.mijing.mapper.VoucherMapper">

    <!--查询商铺下所有优惠券（含秒杀信息）-->
    <select id="queryVoucherOfShop" resultType="com.mj.mijing.entity.Voucher">
        SELECT tv.*, tsv.stock, tsv.begin_time, tsv.end_time
        FROM tb_voucher tv
        LEFT JOIN tb_seckill_voucher tsv ON tv.id = tsv.voucher_id
        WHERE tv.shop_id = #{shopId}
    </select>

</mapper>
